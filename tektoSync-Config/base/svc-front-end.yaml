apiVersion: v1
kind: Service
metadata:
  name: front-end
  namespace: dev-sandbox
spec:
  ports:
  - port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    app: coolstore-front-end
  type: ClusterIP
